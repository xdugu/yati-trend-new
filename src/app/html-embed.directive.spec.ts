import { HtmlEmbedDirective } from './html-embed.directive';

describe('HtmlEmbedDirective', () => {
  it('should create an instance', () => {
    const directive = new HtmlEmbedDirective();
    expect(directive).toBeTruthy();
  });
});
