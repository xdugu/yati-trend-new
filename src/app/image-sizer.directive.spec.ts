import { ImageSizerDirective } from './image-sizer.directive';

describe('ImageSizerDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageSizerDirective();
    expect(directive).toBeTruthy();
  });
});
