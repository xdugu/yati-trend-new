import { FitToChildDirective } from './fit-to-child.directive';

describe('FitToChildDirective', () => {
  it('should create an instance', () => {
    const directive = new FitToChildDirective();
    expect(directive).toBeTruthy();
  });
});
